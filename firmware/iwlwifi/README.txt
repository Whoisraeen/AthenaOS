# iwlwifi ucode goes here ONLY if an Intel AX2xx card is installed.
# Athena's built-in Wi-Fi is a MediaTek MT7902 (14C3:7902) which has NO
# linux-firmware blob and NO mainline Linux driver — see docs/FIRMWARE.md
# (Wi-Fi section) and docs/ATHENA_GROUND_TRUTH.md. Ethernet (RTL8125) is
# the working network path on this SKU.
