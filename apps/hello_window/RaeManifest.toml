# RaeManifest.toml — AthenaOS app permission manifest (Phase 9.2)
# Strict = the unverified-sideload posture: all gated syscall classes denied,
# and [permissions] grants are deliberately ignored at this level (fail-close).

name = "hello_window"
version = "1.0.0"
sandbox = "strict"
