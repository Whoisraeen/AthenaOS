[package]
name = "rae_abi"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "AthenaOS ABI — the single, version-locked serialization point. Frozen syscall numbers, sys_claim_device contract, capability + IPC surfaces every slice imports. OPUS IS SOLE EDITOR; changes are deliberate, batched, sign-off-gated events."

# No dependencies on purpose: this is the contract every slice depends on, so it
# must depend on nothing. #![no_std], const-only. Keep it tiny.
[dependencies]
