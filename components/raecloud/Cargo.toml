[package]
name = "raecloud"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "RaeCloud — cloud storage and sync for RaeenOS"

[dependencies]
spin = "0.9"
# rae_crypto — real ChaCha20-Poly1305 AEAD for client-side blob encryption
# (KeyManager::encrypt_blob/decrypt_blob). no_std + alloc.
rae_crypto = { path = "../rae_crypto" }
