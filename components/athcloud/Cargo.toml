[package]
name = "athcloud"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "RaeCloud — cloud storage and sync for AthenaOS"

[dependencies]
spin = "0.9"
# ath_crypto — real ChaCha20-Poly1305 AEAD for client-side blob encryption
# (KeyManager::encrypt_blob/decrypt_blob). no_std + alloc.
ath_crypto = { path = "../ath_crypto" }
