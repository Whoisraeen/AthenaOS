[package]
name = "raeid"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "RaeID — account system. Passkeys first, optional, never required for local use."

[dependencies]
# Shared memory-hard KDF (Argon2id, RFC 9106) — account passwords hash here
# instead of raeid's former homebrew mixing function.
rae_crypto = { path = "../rae_crypto" }
