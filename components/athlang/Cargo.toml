[package]
name = "athlang"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Rae script — the Swift-flavored automation language (lexer, parser, fuel-limited tree-walk interpreter). no_std; shared by the kernel scripting layer and the userspace athlang daemon."

[dependencies]
