[package]
name = "raefat"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "FAT12/16/32 helpers for ESP/install media (R09, installer-only)."

[dependencies]
