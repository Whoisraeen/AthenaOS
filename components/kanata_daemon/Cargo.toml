[package]
name = "kanata_daemon"
version.workspace = true
edition.workspace = true
license = "LGPL-3.0-only"
authors.workspace = true
description = "Userspace Kanata macro input daemon"

[dependencies]
# kanata-parser requires std. It is vendored but disabled until Phase 11 AthBridge brings full std support.
kanata-keyberon = { path = "vendor/kanata-keyberon" }
raekit = { path = "../raekit" }
