[package]
name = "athstore"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "AthStore — app store. 12% revenue share, sideloading allowed."

[dependencies]
# Real SHA-256 + Ed25519 signature verification for package integrity /
# authenticity — replaces the former homebrew hash + signature-ignoring stub.
ath_crypto = { path = "../ath_crypto" }
