[package]
name = "athbackup"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "RaeBackup — backup and restore system for AthenaOS"

[dependencies]
# Real Argon2id (password KDF), SHA-256 (content hashes), and HMAC-SHA256
# (auth tags) for backup integrity — replaces the former homebrew LCG/FNV stubs.
ath_crypto = { path = "../ath_crypto" }
