[package]
name = "athpkg"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "RaePkg — host-side packer for AthenaOS app bundles (the REBP manifest the kernel's app_bundle loader verifies)"

[dependencies]
